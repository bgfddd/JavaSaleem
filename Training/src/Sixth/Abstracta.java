package Sixth;

public interface Abstracta {
	public void add();
	
	public abstract void subtraction();
	//Interface:
	/*we declare interface when all the methods are abstract.
	 * we able to create method without abstract keyword inside interface because it abstract by nature
	 * whatever variable we create in interface is final, we don't change its value
	 * Multiple inheritance is possible through
	 * We can't create object*/
	int d=6;

}
